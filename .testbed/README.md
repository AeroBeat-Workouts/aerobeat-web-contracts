# Contracts Testbed

This hidden testbed owns contract examples, validation fixtures, demos, and generated local dependency symlinks.

Create `.testbed/node_modules/@aerobeat/web-contracts` as a local symlink to `../../../src` when browser or package-surface tests need it. Do not commit generated `.testbed/node_modules` contents.
