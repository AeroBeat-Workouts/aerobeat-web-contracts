// @ts-check

/**
 * Playwright config placeholder for future browser-visible contract fixtures.
 * Future tests must fail on unexpected console warnings and errors.
 *
 * @type {{ testDir: string }}
 */
export default {
  testDir: "./test"
};
